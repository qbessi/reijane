import type { AstroConfig, IntegrationRouteData } from 'astro';
interface VercelRoute {
    src: string;
    methods?: string[];
    dest?: string;
    headers?: Record<string, string>;
    status?: number;
    continue?: boolean;
}
export declare function escapeRegex(content: string): string;
export declare function getRedirects(routes: IntegrationRouteData[], config: AstroConfig): VercelRoute[];
export {};
